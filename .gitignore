# Dependencies
node_modules/

# Database & sensitive runtime data — NEVER commit these
data/shift.db
data/secret.key
data/photos/
data/cert.pem
data/key.pem

# Keep the data directory itself (server needs it)
!data/.gitkeep

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
